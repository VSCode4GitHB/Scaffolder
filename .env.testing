# Configuration de test
APP_ENV=testing
APP_DEBUG=true

# Base de données de test (SQLite en mémoire)
DB_DRIVER=sqlite
DB_NAME=:memory:

# Désactivation des services externes
MAIL_DRIVER=array
CACHE_DRIVER=array
QUEUE_CONNECTION=sync

# Configuration de test
DEV_MOCK_EXTERNAL_SERVICES=true
DEV_DEBUG_QUERIES=true